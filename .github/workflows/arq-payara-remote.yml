name: arq-payara-remote

on:
  push:
    paths-ignore:
      - "docs/**"
    branches:
      - master
  pull_request:
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      payara:
        image: payara/server-full:7.2026.7
        ports:
          - 4848:4848
          - 8080:8080
        options: >-
          --health-cmd "curl -f http://localhost:4848/ || exit 1"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 20

    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK
        uses: actions/setup-java@v6
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Build with Maven
        run: mvn clean verify -Parq-payara-remote

      - uses: actions/upload-artifact@v7
        if: failure()
        with:
          name: payara-remote-logs
          path: |
            **/logs/
            target/**/*.log
